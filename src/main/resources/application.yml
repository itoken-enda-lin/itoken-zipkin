spring:
  application:
    name: itoken-zipkin
  boot:
    admin:
      client:
        url: http://localhost:8084
  cloud:
    config:
      uri: http://localhost:8888
      name: itoken-zipkin
      label: master
      profile: dev

server:
  port: 9411

management:
  metrics:
    web:
      server:
        auto-time-requests: false